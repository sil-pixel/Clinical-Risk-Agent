# RAG Implementation Decisions

Status: Product-owner-approved baseline on 2026-09-24; model revisions, thresholds, and public release require measured evidence.

Owner: RAG Engineer. Source architecture: [`APPROVED_AI_ARCHITECTURE.md`](APPROVED_AI_ARCHITECTURE.md) and [`AI_MODEL_BENCHMARK_REPORT.md`](AI_MODEL_BENCHMARK_REPORT.md).

## Current handoff (2026-09-25)

Completed checkpoint: [21-source benchmark report](RAG_GENERAL_ASSOCIATION_21_SOURCE_BENCHMARK_REPORT.md), using [locked provisional AI labels](RAG_GENERAL_ASSOCIATION_GOLD_21_SOURCE.json) and their adjacent checksum lock. Same relevance-first RRF/no-cross-encoder configuration for both strategies; diagnostic cosine >0.0 only. Both Recall@5=1.0; document nDCG/MRR=0.9590/0.9737 versus hierarchical 0.9542/0.9474; hierarchical passage BERTScore=0.7796 versus document 0.7597. No unqualified overall winner. Retain the selected hierarchical research baseline and document comparator. Both returned sufficient-evidence status on the no-direct-source control in the original comparison; the subsequent support-gate and claim-support checks are documented below. Runtime >0.85 is unchanged; no architecture threshold sign-off has been issued by this run.

The user subsequently authorized a local MCP playground. [Local stdio MCP instructions](RAG_LOCAL_MCP_PLAYGROUND.md) describe read-only access to the research corpus for evidence search, paper lookup, retrieval comparison and curated-claim inspection. Its contract evaluation passed all 21 exact paper lookups, five positive/four negative curated cases, nine search-provenance checks, five comparison calls and four invalid-input checks on 2026-09-25. This is a developer playground, not approval of the runtime claim gate or user-facing RAG integration; the configured `>0.85` route and architecture sign-off status are unchanged.

Expanded support-gate result: [21-source diagnostic](RAG_RELEVANCE_GATE_21_SOURCE_REPORT.md) covers 19 answerable gold cases, one locked no-direct-source case, and four exploratory endpoint/exposure mismatch probes. At the configured `>0.85` routing threshold, normal retrieval and forced dense-outage fallback each recover direct sources for 19/19 answerable cases and return related sources for 5/5 unsupported questions. A simulated post-selection cosine cutoff has no point that retains all 19 direct answers and abstains on all five unsupported questions. **Decision status: retain `>0.85` and hierarchical retrieval; no new gate or threshold is approved.** Claim-specific support and fallback parity remain integration/evaluation work. Any runtime gate change requires AI Architect sign-off recorded here.

Research integration completed: [claim support and recovery report](RAG_CLAIM_SUPPORT_INTEGRATION_REPORT.md). An optional curated exact-passage assertion checker now filters candidates after both primary and independent fallback retrieval. Five bounded claims and four mismatched SUD claims passed 36 local route checks, plus dual-outage and Qdrant restore checks. The catalog is partial and requires trusted typed claim IDs; it is not activated as a user-facing runtime gate. There is still **no AI Architect sign-off for a new gate policy**. Expand independent passage validation and obtain that sign-off before activation.

The active research corpus now has 21 approved PubMed abstracts: 21 document points and 39 hierarchical passages. Collection IDs, provenance hash, model pins and the next engineering tasks are in [`RAG_ENGINEER_HANDOFF.md`](RAG_ENGINEER_HANDOFF.md). The two- and 16-source builds below describe historical checkpoints.

Product approved proceeding with the existing AI-reviewed judgments and moving human evaluation to a later validation/release workstream. The 20-case judgments remain provisional; a separate AI first pass and an explicit 11-case disagreement record are available. The RAG engineer may consolidate and checksum-lock these labels for reproducible research benchmarks and exploratory gate analysis. Human annotation is not an assignment to the product owner or a prerequisite for this engineering handoff. This workflow update does not approve a runtime threshold change; any such change still requires AI Architect sign-off here. Human-reviewed, held-out and passage-level evaluation remains part of release validation.

## Retrieval strategies to compare

Use the same eligible source set, query judgments, dense encoder, lexical tokenizer, fusion constant, cross-encoder, source cap, and latency environment for both strategies.

| Strategy | Indexed unit | Citation and generation unit |
| --- | --- | --- |
| `document` | Whole title-plus-abstract or one licensed full-text section | Exact matched document/section text, bounded for context |
| `hierarchical` | Sentence-bounded child passage within an abstract or licensed section | Exact matched child passage, with bounded neighboring parent context |

The hierarchical strategy is the implementation baseline; document-level is the controlled comparator. An abstract shorter than the target child size remains one child. No chunk crosses publication identity or a licensed section boundary. Stable document, parent, and child IDs preserve citation identity. Chunk sizes are measured with the selected embedding tokenizer at indexing time: abstract children target 220–300 tokens with one-sentence overlap; full-text children target 300 tokens with no more than 50-token overlap. A tokenizer-independent approximation is permitted only in deterministic local fixtures, not in a benchmark claim.

MedCPT's article encoder is trained and documented with title-plus-abstract input. Title-plus-child embedding is therefore a benchmark hypothesis, not an assumed quality improvement. Compare the two retrieval strategies with relevance labels before pinning the representation.

## Common retrieval path

1. Apply topic, publication date, source class, DOI/PMID, license, quality, metadata isolation, and current retraction checks before indexing and again before returning evidence. PubMed abstracts are the default. Full text requires an explicit machine-readable license permitting storage and display.
2. Search the eligible scientific corpus with MedCPT dense and BM25 lexical retrieval, taking 40 candidates from each. Fuse ranks with RRF `k=60` and retain 30 candidates. Pin a Qdrant version supporting explicit RRF `k`; do not use its default value.
3. Use the selected no-cross-encoder research configuration. MedCPT and MiniLM-L6 were evaluated as alternatives in the historical reranker comparison below; no cross-encoder is required by the continuing research path.
4. Reject candidates below the pinned embedding-specific semantic gate. Apply evidence hierarchy, recency, and quality only to already relevant eligible candidates. Collapse to at most five distinct publications, preserving conflict positions when supported.
5. Return exact matched text and source-owned citation metadata. The LLM may cite only returned source IDs; it may not generate metadata or change excerpt text.
6. If the primary vector path fails or returns no eligible match, run one search against an independently available, same-corpus BM25 index. Keep fallback provenance and the same eligibility gates. Distinguish no eligible evidence from retrieval unavailable.

The initial `>0.85` dense cosine threshold is a release hypothesis. It must be evaluated against relevant, borderline, and irrelevant labels; a threshold change requires AI Architect approval. The two strategies are also compared under dense-only, BM25-only, hybrid, and hybrid-plus-reranker ablations.

## Evaluation and provenance

Freeze a non-user query set with source-level and passage-level relevance judgments, no-evidence cases, and conflicting-evidence cases. Report Recall@5/10/20, Precision@5, MRR@10, nDCG@10, no-evidence precision/recall, conflict coverage, fallback recovery, index size, peak memory, and cold/warm latency. A reranker must improve nDCG@10 or Precision@5 by at least 5% relative without violating the 60-second end-to-end gate. Publish source identities, licenses, model revisions/checksums, corpus/index versions, appraisal rubric, and retraction-check version in the benchmark manifest; do not include runtime user queries or results.

Incremental ingestion and complete active DOI/PMID retraction scrubbing run every two weeks. A retraction check older than 14 days makes a source ineligible. A newly retracted or deprecated source is removed from both active vector and lexical indexes and from any current context window, with a non-retrievable audit tombstone.

## Implementation order

1. Canonical typed retrieval contracts and deterministic eligibility/chunking logic.
2. Versioned public-corpus ingestion and independent BM25 index, with tests for license, quality, recency, retraction, and citation provenance.
3. Qdrant dense/sparse adapter and locally hosted embedding/reranking adapters, pinned by revision and checksum.
4. Same-corpus document-versus-hierarchical benchmark and quality/latency report.
5. Live PubMed escalation, fortnightly update/scrub automation, and downstream handoff.

No exact model or retrieval strategy is represented as empirically superior until the frozen benchmark is run.

## Implementation checkpoint (2026-09-24)

The repository now contains typed retrieval contracts, strict scientific-source eligibility, document and hierarchical chunking, a versioned in-memory corpus snapshot, independent BM25, a Qdrant dense/sparse adapter with mandatory isolation filters, RRF-60 fusion, bounded reranking, exact-text citation records, lexical fallback, and a same-case strategy comparison harness. Local-only MedCPT query/article and cross-encoder adapters require SHA-256-verified safetensors artifacts. PubMed EFetch ingestion accepts only explicitly approved PMIDs and quality appraisals. These paths have synthetic unit and local-Qdrant tests; they are engineering coverage, not a clinical or retrieval-quality result.

Conflict status requires a pre-appraised stance tied to the request's `claim_id`; absent a matching claim, no conflict is inferred from wording. This protects against falsely treating a publication's position as universal across questions.

**Not yet release evidence:** a representative topic-curated, licensed, independently appraised publication set beyond the 16-source research corpus; persisted/reloadable independent BM25 artifacts; a pinned second biomedical embedding comparator and cross-encoder reranker; second reranking configuration; dual-adjudicated source- and passage-level judgments; a release-quality document-versus-hierarchical comparison and ablations; calibrated relevance and no-evidence gates; cold/warm resource results; licensed full-text/BioC path; bounded live PubMed escalation; automated fortnightly ingest/retraction scrub and audit tombstones; downstream workflow integration. The Qdrant adapter has been tested with local Qdrant only, not a deployed service. Public RAG claims and release remain gated on these items and architecture review of any threshold change.

## Local Qdrant and first corpus build

The benchmark store is an embedded, persistent Qdrant database under ignored `data/indexes/qdrant`; no Docker service or cloud account is required. Run `python scripts/rag_provision.py` to open it. This creates storage but intentionally does not create a collection with a guessed vector size or fabricated evidence.

The supplied `data/Research Papers` PDFs are now a **discovery bibliography**, not an exception to the approved ban on directly indexing curated local PDFs. `python scripts/rag_discover_pdfs.py --online` extracts candidate DOIs and resolves PubMed IDs into the ignored `data/indexes/rag_pdf_discovery.json`, then writes `rag_source_manifest_draft.json` with intentionally unapproved entries. The initial scan found 29 PDF files, 23 file-level PubMed matches, and 21 distinct PubMed IDs; duplicate papers and a supplement account for repeated IDs. This is candidate discovery, not source admission: DOI identity, topic, publication date, study design, retraction, and quality must still pass. An initial old-paper spot check exposed and fixed an incorrect fallback to PubMed's processing date in the publication-date parser.

The 21-record provisional screen is recorded in [`RAG_SOURCE_APPRAISALS_DRAFT.json`](RAG_SOURCE_APPRAISALS_DRAFT.json): eight direct-topic candidates, six context-only papers, seven exclusions. It contains source-specific limitations and a design-appropriate appraisal route, but deliberately contains no passing appraisal or numeric quality score. It is not an ingestion manifest. A reviewer must complete full-text appraisal and current retraction checks before any record is marked eligible.

The screen now prioritizes six full-text appraisals immediately (PMIDs 37140715, 38446452, 25830477, 38477889, 38718481, 34262598), two genetics-focused appraisals next (40355602, 41565822), two context appraisals only if the frozen question set requires them (32251439, 32997097), and defers the other four context papers. This is a workload order, not a quality pass or an evidence-hierarchy ranking. The current supplied set remains too narrow in ADHD, ASD, bullying and direct depression evidence for a representative whole-product retrieval benchmark.

A separate PubMed metadata screen now adds 12 non-overlapping topic-expansion candidates, three each for ADHD, ASD, bullying and direct depression evidence, in [`RAG_TOPIC_EXPANSION_CANDIDATES.json`](RAG_TOPIC_EXPANSION_CANDIDATES.json). It includes PMIDs, DOIs, question targets and source-specific appraisal cautions. These are not approved sources: full-text, design-specific quality appraisal, fresh correction/retraction checks and reviewer sign-off still precede ingestion. The balanced shortlist deliberately distinguishes disorder prevalence from symptom prevalence, first onset from recurrence, and association from causal or individual-risk claims.

The first non-user relevance set is drafted in [`RAG_RELEVANCE_JUDGMENTS_DRAFT.json`](RAG_RELEVANCE_JUDGMENTS_DRAFT.json): 24 fixed queries (18 direct, three cross-topic/measurement, three no-evidence) with provisional source grades and claim-level passage targets. It spans the topic-expansion shortlist and six direct empirical candidates from the supplied-PDF screen. It is not benchmark-ready while the approved corpus and exact passage/chunk IDs are absent. The three no-evidence labels mean no answer in this planned corpus, not no scientific evidence anywhere. A depression symptom-versus-diagnosis contrast is not mislabelled as contradictory evidence; conflict coverage stays unscored until a true same-claim opposing pair is appraised. The promotion requirements in the draft require blinded dual review and a pre-run freeze of corpus, models, labels, and IDs.

Start from [`RAG_SOURCE_MANIFEST_TEMPLATE.json`](RAG_SOURCE_MANIFEST_TEMPLATE.json). Each source needs a real PMID, one approved topic, a reviewer, appraisal date, passing score, and rubric version. The template deliberately fails validation until these are filled after review. The ingestion command is:

```sh
python scripts/rag_ingest.py --manifest /path/to/reviewed_sources.json \
  --query-model /path/to/pinned/MedCPT-Query-Encoder --query-sha256 SHA256 \
  --article-model /path/to/pinned/MedCPT-Article-Encoder --article-sha256 SHA256
```

It fetches PubMed metadata/abstracts, rejects any ineligible source, then builds separate immutable document and hierarchical Qdrant collections from the *same* source set. It writes a public-corpus provenance manifest under `data/indexes`. The independent BM25 representation is deterministically rebuilt from the saved passages; production restore/publish automation is still pending. No runtime user material is used. The local store has both the original two-source and expanded 16-source collection versions described below.

On 2026-09-24 the two NCBI MedCPT encoders were downloaded at immutable Hugging Face revisions, their weight and tokenizer/config hashes recorded in [`RAG_MEDCPT_ENCODER_PIN.json`](RAG_MEDCPT_ENCODER_PIN.json), and both loaded offline with 768-dimensional test embeddings. A single-assistant full-text appraisal packet for five prospective first-set papers is in [`RAG_FIRST_SET_FULLTEXT_APPRAISALS.json`](RAG_FIRST_SET_FULLTEXT_APPRAISALS.json). Only the two explicitly approved papers passed the subsequent human sign-off gate; the packet does not treat the other three as eligible.

Silpa signed off PMIDs 37639261 and 39648410 for their specifically bounded uses and approved the binary manifest score rule on 2026-09-24; the other three appraised PMIDs remain unapproved. The signed selections are recorded in [`RAG_FIRST_SET_APPROVED_MANIFEST.json`](RAG_FIRST_SET_APPROVED_MANIFEST.json). Live PubMed EFetch XML on 2026-09-24 matched all five appraised PMIDs and DOIs and reported no retraction/correction flags; the ingestion fetch repeated eligibility and retraction checks for the approved two. These checks must be repeated if stale under the 14-day policy. The score value `1.0` means only that the bounded-use appraisal passed; it is not an additive JBI score or comparative quality estimate.

The first research-only Qdrant build contained those two sources in both strategies: two document points and four hierarchical points. The source owner then approved adding every supplied-PDF paper with a mapped PMID, a publication date inside the rolling 20-year window, and an in-scope direct or context question. The 21-PMID screen yields 14 such papers (eight direct and six context), excluding seven; one duplicate PDF is not a second source. Their targeted full-text, bounded-use assessments are in [`RAG_UPLOADED_PDF_BOUNDED_APPRAISALS.json`](RAG_UPLOADED_PDF_BOUNDED_APPRAISALS.json), and the expanded 16-source selection is in [`RAG_EXPANDED_RESEARCH_MANIFEST.json`](RAG_EXPANDED_RESEARCH_MANIFEST.json). The six context papers are not evidence of individual clinical risk or causal effects merely because they are indexed.

The expanded research-only build is in the **same local Qdrant database**, in new immutable strategy versions: 16 document points and 30 hierarchical points from the same 16 PubMed records. The earlier two-source collections remain as historical versions. The current built versions and PubMed abstract provenance are in `data/indexes/rag_corpus_manifest.json` (local generated artifact, not committed). This is a research benchmark corpus, not clinical-release evidence: the 14 additions received assistant targeted review and Silpa's corpus-inclusion approval, but not independent item-by-item human quality adjudication. Bounded-use interpretation must be enforced during relevance judgment and answering; the current Qdrant payload does not implement question-specific use constraints. No product-facing retrieval may treat these 14 as independently clinically appraised until that release gate is complete.

A pre-run locked, assistant-judged 19-query source-level diagnostic is in [`RAG_BENCHMARK_16_SOURCE_QRELS.json`](RAG_BENCHMARK_16_SOURCE_QRELS.json), with results and limitations in [`RAG_BENCHMARK_16_SOURCE_REPORT.md`](RAG_BENCHMARK_16_SOURCE_REPORT.md). At the configured cosine gate `>0.85`, all 19 queries fell back to BM25; hierarchical achieved Recall@5 of 7/16 versus document 5/16 on answerable cases, but all three no-evidence probes returned evidence. A predeclared dense-threshold ablation is exploratory, not a replacement for threshold calibration or the missing cross-encoder and dual-reviewed passage labels.

The corrected **general-association** comparison is in [`RAG_GENERAL_ASSOCIATION_BENCHMARK_REPORT.md`](RAG_GENERAL_ASSOCIATION_BENCHMARK_REPORT.md), with its pre-run locked [`RAG_GENERAL_ASSOCIATION_GOLD_16_SOURCE.json`](RAG_GENERAL_ASSOCIATION_GOLD_16_SOURCE.json). On 11 answerable abstract-only cases at a diagnostic `>0.0` dense threshold, hierarchical Recall@5 was 7/11 versus document 2/11; all five requested metrics and coverage gaps are reported there. This supersedes the source-lookup diagnostic for general-question comparison but remains provisional, not release evidence.

An exploratory same-hierarchical-corpus reranker comparison is now in [`RAG_HIERARCHICAL_RERANKER_REPORT.md`](RAG_HIERARCHICAL_RERANKER_REPORT.md). It compares relevance-first RRF with MedCPT and MiniLM-L6 cross-encoders on the reused frozen questions. All three found the direct source within five for 11/11 answerable cases, but MedCPT's small top-rank benefit cost far more latency and MiniLM reduced graded nDCG. This is not an untouched confirmatory test. Silpa selected **hierarchical retrieval with relevance-first RRF and no cross-encoder** as the continuing research configuration; `HybridRetriever` now defaults to relevance-first, with evidence tier, recency, and quality used as tie-breakers. Historical benchmark scripts explicitly request the old evidence-first order for reproducibility. The configured `>0.85` cosine gate is unchanged, and no reranker or diagnostic `>0.0` gate is approved for product use. Abstention and independent validation remain open.

The [relevance-gate calibration diagnostic](RAG_RELEVANCE_GATE_CALIBRATION_REPORT.md) swept predeclared cosine cutoffs on that same frozen 13-question set. Scores overlapped between direct-evidence and no-direct-evidence cases; no scalar cutoff retained all 11 direct answers while abstaining on both negatives. The configured `>0.85` routing gate sent all 13 queries to BM25 fallback and abstained on neither negative. **Decision status: no new threshold, no abstention rule, and no runtime gate change approved.** This is an exploratory finding, not a request or record of AI Architect sign-off. A future proposal requires broader independently adjudicated positive/borderline/negative cases, primary/fallback parity, and an explicit AI Architect approval recorded here before changing the deployed gate.
